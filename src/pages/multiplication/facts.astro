---
import PracticeLayout from '@/layouts/PracticeLayout.astro';
import MultiplicationPracticeHub from '@/components/MultiplicationPracticeHub';
import InternalLinks from '@/components/InternalLinks';
import { ORGANIZATION_ID } from '@/config/site';
import '@/styles/practice-surface-prototype.css';

const SITE = 'https://mathpracticeonline.com';

const links = [
  { href: '/multiplication', label: 'Multiplication Hub', description: 'All multiplication modes and resources' },
  { href: '/multiplication/times-tables/1', label: 'Times Tables Practice', description: 'Focus on one table at a time (1–12)' },
  { href: '/division/facts', label: 'Division Facts Practice', description: 'Practice the inverse operation' },
  { href: '/math-worksheets/multiplication-worksheets', label: 'Multiplication Worksheets', description: 'Worksheet-style practice with instant feedback' },
  { href: '/addition/1-digit', label: 'Addition Practice', description: 'Review the foundation for multiplication' },
];

const faqItems = [
  {
    q: 'What are multiplication facts?',
    a: 'Multiplication facts are the 144 products formed by multiplying any two numbers from 1 to 12 (1×1=1 through 12×12=144). Mastering these by memory is called fact fluency.',
  },
  {
    q: 'How many multiplication facts are there from 1 to 12?',
    a: 'There are 144 total facts (a 12×12 grid). Due to the commutative property (3×4 = 4×3), only 78 are truly unique. Most students memorize all 144 so both orientations are automatic.',
  },
  {
    q: 'What grade should students know all multiplication facts?',
    a: 'Most curricula expect fluency with facts through 10×10 by end of Grade 3 and through 12×12 by end of Grade 4.',
  },
  {
    q: 'What is the commutative property of multiplication?',
    a: 'The commutative property means that changing the order of factors does not change the product: 4×7 = 7×4 = 28. Knowing this cuts the number of unique facts to learn nearly in half.',
  },
];
---

<PracticeLayout
  title="Multiplication Facts Practice | Mixed Multiplication 1–12 | Math Practice Online"
  description="Practice mixed multiplication facts from 1×1 to 12×12. Build multiplication fluency with instant feedback and streak tracking."
  canonical={`${SITE}/multiplication/facts`}
  pilotLayout
  breadcrumb={[{ label: 'Home', href: '/' }, { label: 'Multiplication', href: '/multiplication' }, { label: 'Facts' }]}
  breadcrumbVisual={false}
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "LearningResource",
      "name": "Multiplication Facts Practice",
      "description": "Interactive online practice for all multiplication facts from 1×1 through 12×12. Random order drills with instant feedback and progress tracking.",
      "url": `${SITE}/multiplication/facts`,
      "educationalLevel": ["Grade 3", "Grade 4"],
      "teaches": "Multiplication facts 1×1 through 12×12",
      "provider": { "@id": ORGANIZATION_ID }
    })} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": faqItems.map(({ q, a }) => ({
        "@type": "Question",
        "name": q,
        "acceptedAnswer": { "@type": "Answer", "text": a }
      }))
    })} />
  </Fragment>

  <Fragment slot="h1">
    <div class="flex items-center justify-between flex-wrap gap-x-3 gap-y-1">
      <h1 class="text-xl sm:text-2xl font-bold text-[#1E293B] leading-tight">Multiplication Facts Practice</h1>
      <nav aria-label="Change practice" class="font-practice">
        <details class="relative" data-dismissable-menu>
          <summary class="list-none flex items-center gap-1 text-base font-semibold text-[#4F46E5] cursor-pointer select-none px-2 py-1 -mr-2 rounded-lg hover:bg-[#F5F3FF] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#4F46E5] focus-visible:ring-offset-1">
            Change practice
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4" aria-hidden="true"><path d="M6 9l6 6 6-6" /></svg>
          </summary>
          <div class="absolute right-0 top-full mt-1.5 z-20 w-56 max-w-[calc(100vw-2rem)] bg-white border border-[#E4E1F5] rounded-xl shadow-[0_8px_24px_rgba(79,70,229,0.16)] py-1.5">
            <a href="/multiplication/times-tables/1" class="block px-3.5 py-2 text-sm text-[#43405C] hover:bg-[#FAF9FE]">Times Tables</a>
            <a href="/multiplication/facts" aria-current="page" class="block px-3.5 py-2 text-sm font-semibold text-[#4F46E5] bg-[#F5F3FF]">Multiplication Facts</a>
          </div>
        </details>
      </nav>
    </div>
    <script>
      // Same dismissable-menu behavior as the /addition/1-digit and division
      // prototypes' H1-row switcher: closes on Escape (returning focus to the
      // summary), on an outside click, and the moment the user resumes
      // practice (focuses the answer, types, or taps the on-screen keypad)
      // via the `[data-practice-instrument]` marker the practice widget
      // renders itself. See addition/1-digit.astro for the full rationale.
      document.querySelectorAll<HTMLDetailsElement>('details[data-dismissable-menu]').forEach((details) => {
        const summary = details.querySelector('summary');
        const menu = details.querySelector<HTMLElement>(':scope > div');
        const close = () => { details.open = false; };

        const EDGE_MARGIN = 16;
        function clampMenuPosition() {
          if (!menu) return;
          menu.style.left = '';
          menu.style.right = '';
          const rect = menu.getBoundingClientRect();
          const viewportWidth = document.documentElement.clientWidth;
          if (rect.right > viewportWidth - EDGE_MARGIN) {
            menu.style.right = `${Math.max(0, rect.right - (viewportWidth - EDGE_MARGIN))}px`;
          } else if (rect.left < EDGE_MARGIN) {
            const detailsRect = details.getBoundingClientRect();
            menu.style.right = 'auto';
            menu.style.left = `${EDGE_MARGIN - detailsRect.left}px`;
          }
        }
        details.addEventListener('toggle', () => {
          if (details.open) clampMenuPosition();
        });
        window.addEventListener('resize', () => {
          if (details.open) clampMenuPosition();
        });

        document.addEventListener('click', (e) => {
          if (details.open && e.target instanceof Node && !details.contains(e.target)) {
            close();
          }
        });
        details.addEventListener('keydown', (e) => {
          if (e.key === 'Escape' && details.open) {
            e.preventDefault();
            close();
            summary?.focus();
          }
        });
        document.querySelectorAll<HTMLElement>('[data-practice-instrument]').forEach((instrument) => {
          instrument.addEventListener('pointerdown', () => { if (details.open) close(); });
          instrument.addEventListener('focusin', () => { if (details.open) close(); });
        });
      });
    </script>
  </Fragment>

  <Fragment slot="intro">
    <div class="mb-4 bg-[#F8FAFC] border border-[#E2E8F0] rounded-xl px-4 py-3 text-[#1E293B] text-sm sm:text-base leading-relaxed">
      <p>Multiplication facts are the products of two numbers from 1 to 12, such as 6 × 7 = 42. There are 144 total facts in the 1–12 grid, and fluent recall of all of them is the foundation for division, fractions, and multi-digit multiplication.</p>
    </div>
    <p>This page is for third and fourth graders working toward full multiplication fact fluency, as well as parents and teachers who want a broad recall check rather than one table at a time. Problems are pulled from all 144 facts in random order, so success here means genuine recall of the whole 1–12 grid, not memorization of a fixed sequence.</p>
  </Fragment>

  <MultiplicationPracticeHub slot="widget" client:load active="facts" />

  <div slot="how-it-works" class="mt-12 space-y-8">
    <!-- Grade context -->
    <div class="flex items-center gap-2">
      <span class="text-xs font-semibold text-[#6366F1] bg-[#EEF2FF] px-3 py-1 rounded-full uppercase tracking-wide">Best for: Grade 3 – Grade 4</span>
    </div>

    <!-- What you'll practice -->
    <section aria-labelledby="what-heading">
      <h2 id="what-heading" class="text-lg font-semibold text-[#1E293B] mb-3">What You'll Practice</h2>
      <ul class="space-y-2 text-sm text-[#4B5563] leading-relaxed">
        <li class="flex items-start gap-2"><span class="text-[#4F46E5] font-bold mt-0.5">✓</span> All 144 multiplication facts from 1×1 through 12×12</li>
        <li class="flex items-start gap-2"><span class="text-[#4F46E5] font-bold mt-0.5">✓</span> Problems presented in random order to build genuine recall (not rote sequence)</li>
        <li class="flex items-start gap-2"><span class="text-[#4F46E5] font-bold mt-0.5">✓</span> Instant feedback on every answer with streak tracking</li>
        <li class="flex items-start gap-2"><span class="text-[#4F46E5] font-bold mt-0.5">✓</span> Scores and best streak saved in your browser automatically</li>
      </ul>
    </section>

    <!-- Strategies -->
    <section aria-labelledby="strategies-heading">
      <h2 id="strategies-heading" class="text-lg font-semibold text-[#1E293B] mb-3">Mental Math Strategies</h2>
      <div class="grid gap-3 sm:grid-cols-3">
        <div class="bg-white border border-[#E2E8F0] rounded-xl p-4">
          <h3 class="text-sm font-semibold text-[#1E293B] mb-1">Skip Counting</h3>
          <p class="text-sm text-[#6B7280] leading-relaxed">Count up by the multiplier. For 6×7: 7, 14, 21, 28, 35, 42. Works for any table until facts feel automatic.</p>
        </div>
        <div class="bg-white border border-[#E2E8F0] rounded-xl p-4">
          <h3 class="text-sm font-semibold text-[#1E293B] mb-1">Use Commutativity</h3>
          <p class="text-sm text-[#6B7280] leading-relaxed">Know 3×8? You already know 8×3. Turn unfamiliar facts into familiar ones by flipping the order.</p>
        </div>
        <div class="bg-white border border-[#E2E8F0] rounded-xl p-4">
          <h3 class="text-sm font-semibold text-[#1E293B] mb-1">Break It Apart</h3>
          <p class="text-sm text-[#6B7280] leading-relaxed">For 7×8: split as (7×4) + (7×4) = 28 + 28 = 56. Breaking into smaller known facts reduces memory load.</p>
        </div>
      </div>
    </section>

    <!-- Parent & Teacher Guidance -->
    <section aria-labelledby="parent-teacher-heading">
      <h2 id="parent-teacher-heading" class="text-lg font-semibold text-[#1E293B] mb-3">Parent &amp; Teacher Guidance</h2>
      <p class="text-sm text-[#6B7280] leading-relaxed">Random-order practice is intentionally harder than practicing one times table at a time — it tests recall, not sequence memory, and a child can look fluent on a single table while still guessing here. Watch for a child who is fast on some facts (like the 2s, 5s, and 10s) but consistently pauses on others (commonly 6s, 7s, 8s, and 9s); that pattern points to which specific tables need focused practice on the individual times-tables pages rather than more mixed drills. If a child is accurate but slow across the board, they're likely still calculating rather than recalling — short, frequent sessions build recall speed faster than long occasional ones. Mixed practice like this page is best used as a checkpoint after focused single-table practice, not as the first place a new table is learned.</p>
    </section>

    <!-- FAQ -->
    <section aria-labelledby="faq-heading">
      <h2 id="faq-heading" class="text-lg font-semibold text-[#1E293B] mb-4">Frequently Asked Questions</h2>
      <div class="flex flex-col gap-4">
        {faqItems.map(({ q, a }) => (
          <div class="bg-white border border-[#E2E8F0] rounded-xl px-5 py-4">
            <h3 class="text-sm font-semibold text-[#1E293B] mb-1.5">{q}</h3>
            <p class="text-sm text-[#6B7280] leading-relaxed">{a}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- Next step -->
    <p class="text-sm text-[#4B5563]">Want to focus on one table at a time? Try the <a href="/multiplication/times-tables/1" class="text-[#4F46E5] font-semibold hover:underline">times tables practice pages</a> →</p>

    <!-- Internal Links -->
    <InternalLinks links={links} />
  </div>
</PracticeLayout>
